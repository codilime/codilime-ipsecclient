protocol direct {
}

protocol bgp GW {
    ipv4 {
	    export all;
	    import all;
    };
    med metric on;
    passive on;
    local as 65006;
    neighbor range 0.0.0.0/0 external;
}

protocol static {
    ipv4;
    route 100.65.6.0/24 via "lo";
}
