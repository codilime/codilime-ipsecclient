---
- name: configure an example tunnel
  hosts: localhost
  vars_files: ../../vars.yml
  tasks:

  - name: template the configuration json
    ansible.builtin.template:
      src: hw.json.j2
      dest: hw.json

  - name: patch the hardware vrf
    uri:
      url: https://10.67.0.2/restconf/data/sico-ipsec:api/vrf=1
      method: PATCH
      status_code: [200, 204]
      force_basic_auth: yes
      user: "{{ sico_username }}"
      password: "{{ sico_password }}"
      src: hw.json
      validate_certs: no
